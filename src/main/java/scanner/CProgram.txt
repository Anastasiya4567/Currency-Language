import "currencies.json";

Currency exchange(Currency cur, String currType) {
	return abc(cur);
}

int main() {/*example comment*/
	Currency dol = USD(8);
	Currency eu = USD(2EUR);
	Currency sum = dol + eu;
	Currency[] currs = {2PLN, 1USD, 3USD};

	for(int i = 0; i<3; i=i+1;) {
		if (getCurrency(currs[i]) != "EUR" || dd > dw && dw < dw ) {
			currs[i] = exchange(currs[i], "EUR");
		}
		print(currs[i], ", ");
	}
	return 0;
}
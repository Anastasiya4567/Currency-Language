
import currencies.json;

Currency exchange(Currency cur, String val) {
	return EUR(cur);
}

int main() {
	Currency dol = USD(2);
	Currency eu = USD(2EUR);
	Currency sum = dol + eu;
	
	Currency[] valutes = {2PLN, 1USD, 3USD};

	for(int i = 0; i<3; i=i+1) {
		if (getCurrency(valutes[i]) != "EUR") {
			valutes[i] = exchange(valutes[i], "EUR");
		}
		print(valutes[i], ", ");
	}
	return 0;

}